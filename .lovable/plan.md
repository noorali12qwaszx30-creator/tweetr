## الهدف
تصدير **كل** طلبات السائق سجاد جبار (user_id: `e88421cf-86f4-4079-9235-d7112c42b8c9`) من قاعدة البيانات الحقيقية إلى ملف Excel بدون أي حسابات تقديرية.

## مصادر البيانات
- جدول `orders` (الطلبات الحية)
- جدول `orders_history` (الطلبات المؤرشفة)
- جدول `order_items` و `order_items_history` (تفاصيل الأصناف)
- جدول `delivery_areas` (أسماء المناطق)

## محتوى الملف (`sajjad_jabbar_orders.xlsx`)

**الصفحة 1: كل الطلبات** — صف لكل طلب:
- رقم الطلب، التاريخ والوقت (بتوقيت بغداد)، الحالة، النوع (توصيل/تيك أواي)
- اسم العميل، الهاتف، العنوان، المنطقة
- إجمالي السعر، أجرة التوصيل، المستحق للمطعم
- وقت الإنشاء، وقت التسليم، وقت الإلغاء، سبب الإلغاء
- المصدر، ملاحظات

**الصفحة 2: تفاصيل الأصناف** — صف لكل صنف:
- رقم الطلب، اسم الصنف، الكمية، السعر، المجموع، ملاحظات

**الصفحة 3: ملخص يومي** — تجميع حقيقي حسب اليوم:
- التاريخ، عدد المسلّم، عدد الملغى، المحصل، أجرة السائق، المستحق للمطعم

## التسليم
حفظ الملف في `/mnt/documents/sajjad_jabbar_orders.xlsx` وتزويدك برابط التحميل بعد التنفيذ.

هل أبدأ التنفيذ؟